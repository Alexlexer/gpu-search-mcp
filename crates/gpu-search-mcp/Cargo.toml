[package]
name = "gpu-search-mcp"
version = "0.1.0"
edition = "2024"
description = "Experimental Rust MCP scaffold for gpu-search-mcp"
license = "MIT"

[dependencies]
gpu-search-core = { path = "../gpu-search-core" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
